## SWEA 27172 태양광 발전 단지 관제 시스템 D6

#### 2026-08-14 (시간초과)
상기 코드에서 특정 명령만 보더라도 명령수가 15000 번 인것을 알 수 있는데,

update 한번때마다 n ^ 2 시간복잡도가 발생하며 이는 최대 20000 발생한다.

20000 500 500 = 5,000,000,000 -> 시간초과 발생함.

따라서 이 문제에서 펜윅 자체는 적합한 알고리즘이 아니었다.

한마디로 시간복잡도를 고려하지 못했다.

두번째로, 구열별 heap 과 lazy 행열을 도입했다.
이후 상당히 개선이 있었으나,

결국 모든 block 의 top 을 실제로 pop 하는데서 시간초과가 걸렸다.
다른 경우에 매번 query 연산마다 최악 200개 영역을 pop push 하는 로직이 있어서
시간초과를 유발했다.

아래는 정답코드
```python
import heapq


class Point:
    __slots__ = ('x', 'y')          # x: 열(column), y: 행(row)

    def __init__(self, x=0, y=0):
        self.x = x
        self.y = y


# =========================================================
#  여기서부터 5개 함수만 구현하시오.
#  (내장 함수 set과의 충돌을 피하기 위해 Python 뼈대에서는
#   set_value / get_value 라는 이름을 사용한다)
#  main 및 입출력 부분은 수정하지 않는 것을 권장한다.
# =========================================================


def init(N, K, graph):
    # TODO: 각 테스트 케이스 시작 시 1회 호출된다.
    #       전역 자료 구조를 반드시 초기화할 것.
    global board, g_N, hqs, lazy, ver, area, g_K

    board = [row[:] for row in graph]
    g_N = N
    g_K = K
    area = N // K
    lazy = [[0] * area for _ in range(area)]
    hqs = [[] for _ in range(area * area)]
    ver = [[0] * N for _ in range(N)]

    def check_area(i, j):
        row = i // g_K
        col = j // g_K

        return row * area + col

    for i in range(N):
        for j in range(N):
            cur_area = check_area(i, j)
            cur_val = board[i][j]
            heapq.heappush(hqs[cur_area], [-cur_val, j, i, 0])


def set_value(p, value):
    # TODO: 지점 p의 값을 value로 설정(덮어쓰기)
    # 업데이트,
    cy = p.y
    cx = p.x
    board[cy][cx] = value - lazy[cy // g_K][cx // g_K]

    def check_area(i, j):
        row = i // g_K
        col = j // g_K

        return row * area + col

    cur_area = check_area(cy, cx)
    cur_ver = ver[cy][cx]
    heapq.heappush(hqs[cur_area], [-board[cy][cx], cx, cy, cur_ver + 1])
    ver[cy][cx] += 1

    pass

def get_value(p):
    # TODO: 지점 p의 현재 값을 반환
    cy = p.y
    cx = p.x
    # print()
    # for i in board:
    #     print(i)
    # print("lazy")
    # for i in lazy:
    #     print(i)
    return board[cy][cx] + lazy[cy // g_K][cx // g_K]

def update(A, B, num):
    # TODO: [A, B] 범위(구역 정렬 보장)의 모든 지점에 num을 더함
    x1 = A.x
    y1 = A.y
    # A 가 속한 구역의 시작지점 체크해야함.
    x2 = B.x
    y2 = B.y
    
    # x1 부터 x2 까지 area 만큼 옮겨가면서 넣기
    #
    for x in range(x1, x2, g_K):
        for y in range(y1, y2, g_K):
            lazy[y // g_K][x // g_K] += num
    #         print("x, y", x, y)
    # for i in lazy:
    #     print(i)


def query(A, B, count, result):
    # TODO: 우선순위 상위 count개 지점의 좌표를
    #       result[0..count-1]에 채움 (result[i]의 x, y를 덮어쓸 것)
    x1 = A.x
    y1 = A.y

    x2 = B.x
    y2 = B.y
    hq = []
    def check_area(i, j):
        row = i // g_K
        col = j // g_K

        return row * area + col
    cand = []
    cand2 = []
    for x in range(x1, x2, g_K):
        for y in range(y1, y2, g_K):
            cand.append([y, x])
            cand2.append(check_area(y, x))
    cnt = 0
    pop_list = []
     # -------------------------------------------
    # 1. 각 block의 최고값을 "peek"만 해서 hq에 넣기
    # -------------------------------------------
    for c in cand2:

        # stale 데이터만 제거
        while hqs[c]:
            value, cx, cy, version = hqs[c][0]

            if ver[cy][cx] == version:
                break

            heapq.heappop(hqs[c])

        # 정상 top은 꺼내지 않고 보기만 함
        if hqs[c]:
            value, cx, cy, version = hqs[c][0]

            heapq.heappush(
                hq,
                [
                    value - lazy[cy // g_K][cx // g_K],
                    cx,
                    cy,
                    c
                ]
            )

    # -------------------------------------------
    # 2. 전체 후보 중 상위 count개 선택
    # -------------------------------------------
    for cnt in range(count):

        value, cx, cy, c = heapq.heappop(hq)

        result[cnt].x = cx
        result[cnt].y = cy

        # 지금 선택된 원소를 해당 block heap에서 실제로 제거
        block_value, bx, by, version = heapq.heappop(hqs[c])

        # query 끝나고 복구해야 하므로 저장
        pop_list.append([block_value, bx, by, version, c])

        # ---------------------------------------
        # 해당 block의 다음 후보 찾기
        # ---------------------------------------
        while hqs[c]:
            next_value, nx, ny, next_version = hqs[c][0]

            # stale이면 영구 제거
            if ver[ny][nx] != next_version:
                heapq.heappop(hqs[c])
                continue

            # 정상값이면 peek만 해서 global heap에 추가
            heapq.heappush(
                hq,
                [
                    next_value - lazy[ny // g_K][nx // g_K],
                    nx,
                    ny,
                    c
                ]
            )
            break

    # -------------------------------------------
    # 3. query에서 실제로 뽑았던 정상 데이터 복구
    # -------------------------------------------
    for value, cx, cy, version, c in pop_list:
        heapq.heappush(hqs[c], [value, cx, cy, version])


# ========= 이하 수정 비권장 (출력 형식 유지) =========

def main():
    out = []

    T = int(input())
    for tc in range(1, T + 1):
        N, K, M = map(int, input().split())
        graph = [list(map(int, input().split())) for _ in range(N)]

        init(N, K, graph)
        out.append('#%d' % tc)

        for _ in range(M):
            cmd = list(map(int, input().split()))
            op = cmd[0]

            if op == 1:
                _, x, y, v = cmd
                set_value(Point(x, y), v)

            elif op == 2:
                _, x, y = cmd
                out.append(str(get_value(Point(x, y))))

            elif op == 3:
                _, x1, y1, x2, y2, w = cmd
                update(Point(x1, y1), Point(x2, y2), w)

            else:
                _, x1, y1, x2, y2, c = cmd
                res = [Point() for _ in range(c)]
                query(Point(x1, y1), Point(x2, y2), c, res)
                out.append(' '.join('%d %d' % (p.x, p.y) for p in res))

    print('\n'.join(out))
main()
```

#### 2026-08-22 (시간초과)
1. heap을 맨위만 보는 처리 방식을 구현해내는데 실패했다.
간단하다. 아래와 같이 하면 된다.
기존 내코드는.
아래와 같다.
```python
while lazy_hq[y][x]:
    val, cur_x, cur_y, cur_version = heapq.heappop(lazy_hq[y][x])

    if cur_version != version[cur_y][cur_x]:
        continue

    real_val = val - lazy[y][x]

    heapq.heappush(
        temp_hq,
        [real_val, cur_x, cur_y, x, y, cur_version]
    )

    removed.append(
        [val, cur_y, cur_x, y, x, cur_version]
    )
    break
```
이게 왜 문제냐면,
시간복잡도가 문제인데,
heappop() 의 시간복잡도가 O(log H) 라서이다.
그리고 제거한 정상 데이터를 쿼리 종료 후에 다시 temp 안에 집어넣기 위해 heappush() 까지 해야해서

O(2log H) 의 시간복잡도 가 발생한다.
근데 여기서 그럼 H는 구역이니까 400 이다. 아니다.
H는 약 구역하나의 힙에 들어있는 원소 갯수이다.
그냥 보기만 하면 O(1) 인데 여기서 큰 비용차이가 크다.  
왜냐하면, 매번 다 한번씩 pop을 하게 되기 때문이다.

아래 코드로 하면 한번에 heappify, 그리고 맨앞만 보고 후보군 선정 및 삭제가 가능하다.

```python
    # 1. 각 구역의 1등을 temp_hq에 넣는다.
    for sec_y in range(start_y_sec, end_y_sec + 1):
        for sec_x in range(start_x_sec, end_x_sec + 1):

            hq = lazy_hq[sec_y][sec_x]

            # stale 제거
            while hq:
                val, x, y, ver = hq[0]

                if ver != version[y][x]:
                    heapq.heappop(hq)
                else:
                    break

            if not hq:
                continue

            val, x, y, ver = hq[0]

            # 실제 값의 음수
            real_val = val - lazy[sec_y][sec_x]

            temp_hq.append(
                (real_val, x, y, sec_y, sec_x)
            )

    # 하나씩 heappush 하지 않고 한번에 heapify
    heapq.heapify(temp_hq)

    # 2. 전체 구역을 대상으로 상위 count개 뽑기
    for idx in range(count):

        real_val, x, y, sec_y, sec_x = heapq.heappop(temp_hq)

        result[idx].x = x
        result[idx].y = y

        hq = lazy_hq[sec_y][sec_x]

        # 현재 선택된 놈 제거
        item = heapq.heappop(hq)

        # query 끝난 후 다시 복구
        removed.append((sec_y, sec_x, item))

        # stale 제거
        while hq:
            val, nx, ny, ver = hq[0]

            if ver != version[ny][nx]:
                heapq.heappop(hq)
            else:
                break

        # 해당 구역의 다음 후보 등록
        if hq:
            val, nx, ny, ver = hq[0]

            next_real_val = val - lazy[sec_y][sec_x]

            heapq.heappush(
                temp_hq,
                (next_real_val, nx, ny, sec_y, sec_x)
            )

    # 3. query 때문에 뽑았던 정상 데이터 복구
    for sec_y, sec_x, item in removed:
        heapq.heappush(
            lazy_hq[sec_y][sec_x],
            item
        )
```
