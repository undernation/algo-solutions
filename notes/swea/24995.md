## SWEA 24995 온라인마트

#### 2026-08-20 (못품)
못푼 이유.
1. 가격을 lazy deletion 으로 관리하는 개념을 떠올리긴 했으나, 실행하지 못함.
2. hq 가 0부터 정렬된다고 착각하였음.
3. copy 를 동일하게 남발함.
 카피의 경우 가능한 사용하지말고, 뺀거에서 다시 넣어주는 방식을 사용하는것이 좋다.
생각보다 어렵지 않다.

힙안에는 원래 저장된 값만 관리하면 되었다.
살아있는 숫자, active count 를 따로 관리하는 개념을 떠올리지 못함.

시간복잡도를 제대로 계산하지 못했음.

대표적으로
1. discount()
맨 처음엔
```python
size = len(matrix[c][company])

for idx in range(size):
    cur_price, cur_id = matrix[c][company][idx]
    ...
```

위 코드는 상품 K개면 O(K) 이고, 할인 10000번 들어오면 최악 1억임.
 개선후에는 가격 0 이하 된것만 체크해서 빼주고 멈춘다.
기존:
D × K

현재:
D + N log N 정도
(수정전)
discount 10000번 호출되고, 상품만큼 순환일어나므로 무조건 시간초과.
```python
def discount(mCategory : int, mCompany : int, mAmount : int) -> int:
    # 특정 범주 상품의 가격 mAmount 만큼 낮춘다.
    # 낮춘게 0이거나 음수되면 해당 상품 판매 종료.
    # 낮춘 후 품목이 mCate 이고 제조사 mcom 판매되는 상품 '개수' 반환
    cnt = 0
    size = len(matrix[mCategory][mCompany])

    for idx in range(size):
        cur_price, cur_id = matrix[mCategory][mCompany][idx]
        new_price = cur_price - mAmount

        if new_price <= 0:
            product_price[cur_id] = 0
        else:
            product_price[cur_id] = new_price
            cnt += 1

    print("discount", cnt)
    return cnt
```
(수정 후)
lazy로 더해줘서 괜찮음.
거기에다가 가격 낮은것 기준으로다가 죽은 애만 순회함.
힙순회라 O(log N). 따라서 NlogN 이라서 괜찮음. log₂(10000) ≈ 13.29
```python
def discount(mCategory : int, mCompany : int, mAmount : int) -> int:
    # 특정 범주 상품의 가격 mAmount 만큼 낮춘다.
    # 낮춘게 0이거나 음수되면 해당 상품 판매 종료.
    # 낮춘 후 품목이 mCate 이고 제조사 mcom 판매되는 상품 '개수' 반환
    discount_sum[mCategory][mCompany] += mAmount

    while matrix[mCategory][mCompany]:
        cur_price, cur_id = matrix[mCategory][mCompany][0]
        # closeSale 등으로 이미 죽었던 상품
        if product_info[cur_id][3] == False:
            heapq.heappop(matrix[mCategory][mCompany])
            continue
        if cur_price - discount_sum[mCategory][mCompany] <= 0:
            heapq.heappop(matrix[mCategory][mCompany])
            product_info[cur_id][3] = False
            active_count[mCategory][mCompany] -= 1
        else:
            break

    # print("discount", cnt)
    return active_count[mCategory][mCompany]

```

#### 2026-08-27 (못품)
못푼이유:
discount 의 시간복잡도가 50000 * 10000 일거로 착각하고
지레 겁먹고 구현을 포기함.
