## SWEA 25958 타워디펜스게임

#### 2026-09-13 (틀림)
상태 조건을 깔끔하게 짜지 못했다.
차라리 막히면 문제있는 함수를 재구현하는게 나을수도 있을것 같다.

틀린 이유는 명확히는 같은 타겟을 설정했을 때
타워 쿨타임을 돌리지 않는 실수를 범했다.
```python
        if can_attack(cur_tower_y, cur_tower_x, last_target_y, last_target_x):
            targets.append(last_target)
            cur_tower.next_shoot_time = cur_time + cur_tower.reload_time
            return
```
상기 부분이다.

그리고 두번째로, 타워의 상태에서 마지막으로 공격한 id 를 초기화하지 않는 실수를 범했다.

조건들을 자세하게 구현하지 못한 탓이다.
