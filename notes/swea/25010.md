## SWEA 25010 인기 검색어

#### 2026-08-31 (못품)
1. 이 문제는 마스킹을 활용한다.

목표는 두가지다.
1. 검색어의 등록

2. top 5 검색어 찾기
위 top 5 의 기준은 많이 등장한순, 그다음은 알파벳순.

유사한 단어들은 하나의 그룹이 된다.

대표 검색어
- 등장 횟수
- 사전순으로 앞선 검색어

그럼 단어가 나오면 이걸 어떻게 등록할까?

1. 등장횟수 dict 에 등록한다

여기서 막혔다. 복습해볼것.

#### 2026-09-05 (못품)
접근조차 못함.
이전보다는 약간 코드가 읽히는 것 같다.

activate 랑
deactivate 함수를 정의한다.

각각 그안에서 getpatterns 함수를 사용한다
getpatterns 함수는 단어 넣으면 return 으로 마스킹된 애들 돌려주는 코드이다.
activate 하면 pattern_dict 에 pattern 넣고
deactivate 하면 bucket 호출 후 그안에 word 각각 지워준다.
버컷이 비어있으면 버켓 자체를 del 한다.
top5 keyword 함수는 keyword_cnt 를 순회하면서
각각 키워드를 순회한다.
representative 랑 representative_count 를 바꿔준다(대표단어)

여기까지 이해했다.
