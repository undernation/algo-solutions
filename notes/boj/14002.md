## BOJ 14002 가장 긴 증가하는 부분 수열 4

#### 2026-09-25 (못품)
아직 lis 사용이 익숙하지 않는다

i 로 순회하는데 그안에서 이중 for 문으로 j 를 순회한다.
j가 만약에 i 보다 작은 경우 i 앞에 붙일 수 있으므로
dp[i] 의 값에 1을 더해준거 그리고 원래 dp[i] 값이랑 max 를 찾는다.
그리고 이경우에만 parent 에 업데이트를 해준다.
