## BOJ 17143 낚시왕

#### 2026-08-18 (품)
맞았으나., 주기 운동은 % 2 * (R - 1) 해주는 개념을 익히고 있으면 좋다.
```python
def move(sy, sx, direction, speed):
    cy = sy
    cx = sx

    if direction <= 1:
        speed %= 2 * (R - 1)
    else:
        speed %= 2 * (C - 1)

    dy, dx = dydx[direction]

    for _ in range(speed):
        ny = cy + dy
        nx = cx + dx

        if not (0 <= ny < R and 0 <= nx < C):
            direction = change_dir(direction)
            dy, dx = dydx[direction]
            ny = cy + dy
            nx = cx + dx

        cy = ny
        cx = nx

    return cy, cx, direction
```
